FROM python:3.12-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    SRI_WORKER_ALLOWED_AMBIENTES=1 \
    SRI_WORKER_INTERVAL_SECONDS=300

WORKDIR /app

COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

COPY *.py ./

CMD ["python", "-u", "sri_worker.py"]
